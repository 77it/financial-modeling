// run with `deno test THIS-FILE-NAME`

import {assert, assertFalse, assertEquals, assertNotEquals} from "https://deno.land/std/testing/asserts.ts";

import {ModuleData, ModuleDataLoader} from "./_ModuleData.js";

Deno.test("XXX", () => {
    assert(xxx);
});
