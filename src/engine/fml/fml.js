/*
Formula evaluation notes:
Every formula returns a number.
Every formula is evaluated inside a try/catch block.
If an error occurs (e.g. for undefined driver), the error is caught and the formula is evaluated to ZERO.

Uses https://github.com/77it/formula
 */
