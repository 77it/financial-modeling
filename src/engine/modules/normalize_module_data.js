/**
 The function `normalizeModuleData` is not defined here but inside `engine.js` because during Main is be loaded
 from the `engine` URL itself or, as a fallback, from the engine via the function the `ModulesLoader` method `getNormalizeModuleData()`
 */
