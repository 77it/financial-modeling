import {
    assert,
    assertEquals,
    assertFalse,
    assertThrows,
} from 'https://deno.land/std/testing/asserts.ts';


Deno.test('test loan', async (t) => {
    await t.step("test #1", async () => {
        // TODO
    });
});
